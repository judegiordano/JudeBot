# JudeBot

```sh
NODE_ENV=<string> // development | productions
CLIENT_ID=<number> // discord client id
CLIENT_SECRET=<string> // discord client secret
BOT_TOKEN=<string> // discord bot token
SCRYFALL_URI=<string> // scryfall api endpoint
MTG_LOGO=<string> // mtg logo image url
INVITE=<string> // discord bot invite link
AVATAR_URI=<string> // bot avatar image url
```

```sh
npm install
npm run start
```